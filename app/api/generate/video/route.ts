import { NextResponse } from 'next/server';

// TODO: Implémenter la génération vidéo via Replicate (Wan 2.1)
// POST /api/generate/video

export async function POST() {
    return NextResponse.json({ message: 'Video Generation API - En construction' });
}
