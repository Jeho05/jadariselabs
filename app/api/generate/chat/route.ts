import { NextResponse } from 'next/server';

// TODO: Implémenter le chat IA via Groq (LLaMA 3.3 70B)
// POST /api/generate/chat

export async function POST() {
    return NextResponse.json({ message: 'Chat API - En construction' });
}
